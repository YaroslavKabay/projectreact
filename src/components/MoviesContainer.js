import React from 'react';
import {MoviesList} from "./MoviesList";

const MoviesContainer = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {MoviesContainer};